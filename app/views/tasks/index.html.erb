

<h1>Tasks</h1>

<br>

<% if user_signed_in? %>s

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Title</th>
      <th scope="col">User Id</th>
      <th colspam="1"></th>
    </tr>
  </thead>
 
  <tbody>
  <% @tasks.each do |task| %>
    <% if task.user ==
    current_user %>
    <tr>
      <td><%= task.task_title %></td>
      <td>
      <%= link_to task.task_title, task %>
      </td>

      <td><%= task.user_id %></td>
      
      <td><%= link_to "Delete", @task, method: :delete,
      data: { confirm: 'Are you sure?'}, class:"btn btn-outline-danger" %></td>
 
    </tr>
  <% end %>
  </tbody>
  </table>


<%= link_to "+", new_task_path, class:"btn 
btn-secondary" %>

<& else %>

<h1> HOME </h1>



<% end %>

